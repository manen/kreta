[package]
name = "login_test"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
kreta-rs = { workspace = true, features = ["client"] }
tokio = { workspace = true, features = ["rt", "fs"] }
